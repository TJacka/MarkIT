<%- include('partials/header') -%>

  <div class="container card-container">
    <h1 class="mb-4" style="margin-top: -2rem;">Top Swipes From Around The Globe</h1>
    <div class="row row-cols-md-4">
      <% for(var i=0; i<posts.length; i++) {%>
      <div class="col mb-2">
        <div class="card h-100">
          <img src="<%= posts[i].image%>" style="object-fit: contain;" class="card-img-top" alt="...">
          <div class="card-body">
            <h5 class="card-title"><%= posts[i].title%></h5>
            <p class="card-text"><%= posts[i].caption %></p>
            <!-- <p class="card-text"><i class="px-1 bi bi-heart"></i><%= posts[i].likes.length - 1 %></p> -->
            <a href="/post/<%= posts[i]._id%>" class="btn btn-outline-primary">View Swipe</a>
          </div>
          <!-- <div class="card-footer">
            <span class="badge bg-primary"><%= posts[i].category%></span>
          </div> -->
        </div>
      </div>
    <% } %>
  </div>
</div>

<%- include('partials/footer') -%>

